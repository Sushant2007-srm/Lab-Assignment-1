#include<stdio.h>
int main(){
    char str[] = "Mahendra Singh Dhoni";
    int strLength = 0;
    char *ptr;
    ptr = str;
    for (int i = 0; *(ptr + i) != '\0'; i++) {
        strLength++;
    }
    printf("Length of the string is %d", strLength);
    return 0;
}
